query GetBookings($spaceId: String!) {
  getBookings(spaceId: $spaceId) {
    date
    isAvailable
    bookings {
      id
      date
      canCancel
      user {
        fullName
      }
    }
  }
}
